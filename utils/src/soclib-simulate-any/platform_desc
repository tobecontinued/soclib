
# -*- python -*-

vci_params = dict(
	cell_size = 4,
	plen_size = 1,
	addr_size = 32,
	rerror_size = 1,
	clen_size = 1,
	rflag_size = 1,
	srcid_size = 8,
	pktid_size = 1,
	trdid_size = 1,
	wrplen_size = 1
	)

todo = Platform(
	Uses('caba_factory'),
	Uses('vci_factory', **vci_params),
	Source('top.cpp', uses = [
	Uses('topcell', mode = 'common'),
	Uses('exception', mode = 'common'),
	]),
)
