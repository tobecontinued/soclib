
# -*- python -*-


todo = Platform('tlmt', 'top.cpp',
                uses = [Uses('vci_vgmn'),
                        Uses('vci_ram'),
                        Uses('vci_xcache_wrapper',iss_t = 'common:mipsel'),
                        Uses('vci_multi_tty'),
                        Uses('vci_mwmr_controller'),
                        Uses('fifo_reader'),
                        Uses('fifo_writer'),
#                        Uses('coprocessor'),
                        ],
                addr_t = 'uint32_t',
                data_t = 'uint32_t',
                nbytes = '4',
                )
